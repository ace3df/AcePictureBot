credentials = dict(
    consumer_key='',
    consumer_secret='',
    access_token='',
    access_token_secret='',
)

status_credentials = dict(
    consumer_key='',
    consumer_secret='',
    access_token='',
    access_token_secret='',
)

settings = dict(
    twitter_track=['AcePictureBot', 'AceYaoiBot', 'AceYuriBot',
                   'AceEcchiBot', 'AceNSFWBot'],
    image_loc='',
    list_loc='',
    cookie_loc='',
    ignore_loc='',
    count_on=True,
    count_file='count.ini',
    ignore_tags=['Pony'],
    webm_on=True,
    webm_script='script.bat',
)

update = dict(
    auto_update=True,
    python_process=r"python acepicturebot.py",
    is_busy_file=r"is_busy.txt",
    repository=r"https://github.com/ace3df/AcePictureBot.git",
    repository_rss=r"https://github.com/ace3df/AcePictureBot/commits/master.atom",
    recent_commit_file=r"GitCommitUpdate.txt",
    only_restart_on_py=True,  # Only restart if a updated file is .py
)

extra_api_keys = dict(
    pastebin='',
    pasteee='',
    google='',
)

website_logins = dict(
    sankakucomplex_username='',
    sankakucomplex_password='',
)
